'use client';

import MainLayout from "@/components/MainLayout";

export default function CheckinResultLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <MainLayout>{children}</MainLayout>;
} 